services:
  api:
    build: .
    image: travelers_api
    container_name: travelers_api
    restart: unless-stopped
    command: ["fastapi", "run", "/app/main.py", "--port", "8000"]
    ports:
      - "8765:8000"
    volumes:
      - ./app:/app
